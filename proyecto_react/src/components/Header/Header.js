import React from "react";
import "./Header.css"
import Navbar from "../Navbar/Navbar";

const Header = () => {
    return (
    <nav>
        <Navbar user= "Mica" />
    </nav>
    );
  }

export default Header;